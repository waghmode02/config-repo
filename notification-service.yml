spring:
  application:
    name: notification-service

  kafka:
    bootstrap-servers: localhost:9092

notification:
  topic:
    name: order-notification

server:
  port: 8084
spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: waghmodenagesh18@gmail.com
    password: Nagesh@1234
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true  

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
